# 装备环境：
sudo apt-get install -y libssl-dev make build-essential libssl-dev zlib1g-dev libbz2-dev libreadline-dev libsqlite3-dev wget curl llvm python3-tkinter
# install git:
sudo apt-get install git
# 自动安装 pyenv和pyenv-virtual： 
sudo curl -L https://raw.githubusercontent.com/yyuu/pyenv-installer/master/bin/pyenv-installer | bash
# 设置个人用户路径，vi ~/.bash_profile 添加：
# export PATH="$HOME/.pyenv/bin:$PATH"
# eval "$(pyenv init -)"
# eval "$(pyenv virtualenv-init -)
# source ~/.bash_profile
